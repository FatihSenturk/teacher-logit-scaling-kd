# ÜRETİLDİ — elle düzenlemeyin.
# Üretici: diagnostics/requirements_lock.py
# Tanım: deponun .py dosyalarındaki top-level import adları -> kurulu dağıtım
#        (importlib.metadata.packages_distributions), sürüm o anda KURULU olan.
# Python: 3.13.10
# Taranan import adı: 21 · eşlenen dağıtım: 19

# Aşağıdaki pin'lerden bazıları PyPI'de YOK (yerel etiket: cu128).
# Bu satır olmadan `pip install -r` "No matching distribution" ile düşer.
--extra-index-url https://download.pytorch.org/whl/cu128

matplotlib==3.10.8
numpy==2.4.0rc1
opencv-contrib-python==4.13.0.92
opencv-python==4.13.0.92
pandas==2.3.3
pillow==12.1.0
PyMuPDF==1.27.1
PyYAML==6.0.3
scikit-learn==1.8.0
scipy==1.16.3
seaborn==0.13.2
swanlab==0.7.11
thop==0.1.1-2209072238
timm==1.0.24
torch==2.10.0+cu128
torchmetrics==1.9.0
torchvision==0.25.0+cu128
tqdm==4.67.1
wandb==0.25.1

# eşlenemedi (kurulu bir dağıtıma bağlanamayan import adları):
#   reportlab  <- generate_kd_figure.py
#   svglib  <- generate_kd_figure.py
#   svgwrite  <- generate_kd_figure.py
