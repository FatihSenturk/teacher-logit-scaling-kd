# ÜRETİLDİ — elle düzenlemeyin.
# Üretici: diagnostics/requirements_lock.py
# Tanım: README'nin Level-1 için verdiği iki komutun GEÇİŞLİ import kapanışı
#        (diagnostics/paper_tables.py + diagnostics/table_diff_gate.py), yerel modüllere inilerek.
# Python: 3.13.10
# Kapanış: 18 yerel dosya · 14 dağıtım
# Tam ortamdan düşen: PyMuPDF, PyYAML, seaborn, torchmetrics, wandb
# CUDA gerekmez: `torch` kapanışta olduğu için var, GPU istendiği için değil;
# yerel etiket silindi (bkz. üreticinin başlığı).

# Aşağıdaki pin'lerden bazıları PyPI'de YOK (yerel etiket: cu128).
# Bu satır olmadan `pip install -r` "No matching distribution" ile düşer.
--extra-index-url https://download.pytorch.org/whl/cpu

matplotlib==3.10.8
numpy==2.4.0rc1
opencv-contrib-python==4.13.0.92
opencv-python==4.13.0.92
pandas==2.3.3
pillow==12.1.0
scikit-learn==1.8.0
scipy==1.16.3
swanlab==0.7.11
thop==0.1.1-2209072238
timm==1.0.24
torch==2.10.0
torchvision==0.25.0
tqdm==4.67.1
